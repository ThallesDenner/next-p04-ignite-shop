// export const priceFormatter = new Intl.NumberFormat("pt-BR", {
//   style: "currency",
//   currency: "BRL",
// });

// export function convertCurrencyValueToCents(currencyValue: string) {
//   return Math.round(parseFloat(currencyValue.replace("R$", "").replace(",", ".")) * 100);
// }
